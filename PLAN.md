# 藏文佛教经典汉译 Agent —— 建设规划

> 目标：基于已有素材（藏汉/藏英词典、若干已完成的藏文佛经汉译本），构建一个
> 术语一致、文体可控、可人工审校迭代的藏译汉翻译 Agent。

---

## 一、总体思路与关键决策

### 1. 不做微调，走「检索增强 + 多轮流水线」路线

理由：

- 微调（fine-tune）需要数十万句级对齐的平行语料，几本译本的数据量远远不够，
  强行微调只会过拟合、损害模型本身的藏文能力。
- 你手里素材的真正价值在于：**术语的权威译法**（词典）和**文体与译例范式**
  （已完成译本）。这两样恰好是检索增强（RAG）最擅长注入的东西。
- Claude 等大模型本身已具备相当的藏文理解能力，缺的是「你这个传承/体例下的
  规范译法」，这靠检索喂给它即可。

### 2. 翻译不是一步完成，而是一条流水线

```
原文分段 → 检索(词典/术语库/翻译记忆) → 初译 → 术语一致性校验
        → 文体润色(长行/偈颂分开处理) → 人工审校 → 审校结果回流术语库
```

每一步职责单一，出错可定位，人工审校的成果能沉淀下来让系统越用越准。

### 3. 三层知识资产

| 层 | 来源 | 形态 | 作用 |
|---|---|---|---|
| 词典层 | 藏汉/藏英词典 | 结构化词条库（SQLite/JSONL） | 逐词释义、兜底查询 |
| 术语层 | 从已有译本中抽取 + 人工确认 | 术语表（藏文 ↔ 规范汉译 ↔ 梵文原语 ↔ 备选译法） | 强制术语一致性 |
| 译例层 | 已有译本句级对齐 | 翻译记忆 + 向量索引 | 提供文体范式与相似句参考 |

术语层是核心竞争力：同一个 `byang chub sems` 全书必须统一译作「菩提心」，
这是机器翻译最容易翻车、也是佛经翻译最不能翻车的地方。

---

## 二、目录结构（建议）

```
.
├── PLAN.md                    # 本文档
├── data/
│   ├── raw/                   # 原始素材，只进不改
│   │   ├── dictionaries/      # 词典纯文本原件
│   │   ├── parallel/          # 已完成译本（藏文原文 + 汉译）
│   │   └── source_texts/      # 待翻译的藏文经典
│   ├── processed/
│   │   ├── dict.sqlite        # 结构化词典库
│   │   ├── glossary.tsv       # 术语表（人工可直接编辑）
│   │   ├── tm.jsonl           # 翻译记忆（句级对齐对）
│   │   └── embeddings/        # 译例向量索引
│   └── eval/                  # 从译本中留出的评测集（不进翻译记忆）
├── pipeline/
│   ├── normalize.py           # 藏文规范化（Unicode NFC、分句）
│   ├── parse_dict.py          # 词典解析入库
│   ├── align.py               # 译本句级对齐
│   ├── extract_terms.py       # 术语抽取
│   └── build_index.py         # 构建检索索引
├── agent/
│   ├── translator.py          # 主流水线编排
│   ├── tools.py               # 查词典/查术语/查翻译记忆 三个工具
│   ├── prompts/               # 各阶段 system prompt（初译/校验/润色）
│   └── config.yaml            # 文体、模型、体例配置
├── review/
│   ├── export.py              # 导出藏汉对照审校稿（docx/md 双栏）
│   └── ingest_feedback.py     # 审校修改回流术语库
└── eval/
    └── run_eval.py            # 术语一致率 + chrF + 抽样人评
```

---

## 三、分阶段实施步骤

### 阶段 0：素材盘点与规范（1~2 天）

1. 把所有素材按上面 `data/raw/` 结构归位，原件永不修改。
2. 逐个确认词典的行格式（词条分隔符、释义结构、有无缩写体例说明页）。
3. 确认译本情况：是否有对应的藏文原文电子版？对齐粒度能到什么级别
   （品/章、偈颂、句）？
4. 统一编码：全部转 UTF-8，藏文做 Unicode NFC 规范化
   （合字、元音符号存在多种编码序列，不规范化后面检索会全部失配）。

### 阶段 1：数据管道（核心工作量所在，约 1~2 周）

**1a. 词典入库（`parse_dict.py`）**

- 按各词典自身格式写解析器，统一为
  `{headword_bo, definitions[], pos, source_dict}` 结构存入 SQLite。
- 藏文词头额外存一列去掉末尾 tsheg（་）的规范形式，供查询归一。
- 多部词典并存不合并，查询时按来源标注返回，让模型自己权衡。

**1b. 译本对齐（`align.py`）**

- 分句：藏文按 shad（། / ༎）切分，汉文按句号/偈颂行切分。
- 偈颂体（四句一颂）结构规整，优先按颂对齐，准确率高；
  长行（散文）用「章节锚点 + 长度比例 + 词典词命中数」做半自动对齐。
- **对齐质量宁缺毋滥**：低置信度的对齐对宁可丢弃，脏数据进翻译记忆
  比没有数据更糟。产出 `tm.jsonl`：
  `{bo, zh, source_text, location, confidence}`。

**1c. 术语抽取（`extract_terms.py`）**

- 从对齐语料统计高频藏文名词短语与其汉译的共现，候选术语对交人工确认。
- 用 LLM 辅助初筛（给它对齐句对，让它列出术语对应关系），人工终审。
- 产出 `glossary.tsv`（藏文｜规范汉译｜梵文｜备选译法｜备注｜出处），
  **这个文件保持人工可直接编辑**，它是整个系统的宪法。

**1d. 建索引（`build_index.py`）**

- SQLite FTS5 做词典与术语的精确/前缀检索。
- 翻译记忆做向量索引（藏文句 embedding），翻译时召回最相似的 3~5 个译例。

### 阶段 2：Agent 核心（约 1 周）

用 Claude API（推荐 Claude Agent SDK 或直接 Messages API + tool use）搭流水线：

**工具（tools.py）三件套：**

| 工具 | 输入 | 返回 |
|---|---|---|
| `lookup_dict` | 藏文词 | 各词典释义 |
| `lookup_glossary` | 藏文词/短语 | 规范译法（强制遵守） |
| `search_tm` | 藏文句 | 最相似的历史译例（供文体参考） |

**流水线各遍（每遍独立 prompt）：**

1. **预处理遍**：对当前段落做词汇扫描，把命中的术语表条目和词典释义
   预先注入上下文（比让模型自己决定何时查更稳定）。
2. **初译遍**：给出原文 + 术语约束 + 相似译例，要求逐句翻译并保留
   句级对照结构（方便后续审校）。明确指示：术语表有的**必须**用规范译法。
3. **校验遍**：程序化检查——初译中出现的藏文术语是否都用了规范译法、
   有无漏译句（藏汉句数比对）、数字/人名/地名核对；发现问题打回重译该句。
4. **润色遍**：按 `config.yaml` 指定文体处理——
   长行用现代书面语（或仿古译经体，可配置）；
   偈颂可选输出为每颂四句的齐整格式；保持「如是我闻」等定型套语。

**体例配置（config.yaml）**：文体（白话/仿古）、偈颂格式、
敬语处理、音译用字表（陀罗尼咒语音译规范）等，全部显式配置而非隐含在 prompt 里。

### 阶段 3：审校闭环（与阶段 2 并行搭建）

1. `export.py` 输出藏汉逐句对照的双栏审校稿（docx 或 markdown 表格），
   命中术语高亮标注出处。
2. 审校者直接改稿；`ingest_feedback.py` 对比修改：
   - 术语级修改 → 更新 `glossary.tsv`
   - 整句修改 → 修正对加入翻译记忆
3. 每轮翻译前重建索引，系统随审校持续变准。

### 阶段 4：评测（贯穿始终）

1. 从已有译本中**留出**一两品作为评测集（绝不能进翻译记忆，否则是作弊）。
2. 指标分三层：
   - **术语一致率**（自动，最重要）：术语表命中处使用规范译法的比例；
   - **chrF / BLEU**（自动，参考值，佛经文体下绝对值意义有限，看趋势）；
   - **人工抽评**（每轮抽 20~30 句，按「信/达/术语/体例」四维打分）。
3. 每次改 prompt、改流水线、扩术语库后跑一遍评测，防止回退。

---

## 四、藏文处理的几个具体坑（提前避）

1. **Unicode 规范化**：藏文组合字符编码序列不唯一，所有入库文本一律 NFC，
   否则同一个词查不到。
2. **分词**：藏文以 tsheg（་）分音节但不分词。词典查询要做
   **最长匹配切词**（用词典词头本身当词表跑 maximal matching），
   而不是指望按音节查。
3. **格助词黏着**：词尾常黏格助词（如 -འི、-ར、-ས），查词典前要做简单的
   形态还原（剥离常见格标记再查）。
4. **异体与缩写**：古籍常见缩写字（如 ཿ 类缩写）与异体拼写，
   在 normalize 阶段建映射表统一。
5. **梵文转写**：咒语、陀罗尼是梵文藏文转写，不该意译，
   流水线里要识别出来走音译通道（参照既有译本的音译用字）。

---

## 五、技术选型小结

| 事项 | 选择 | 理由 |
|---|---|---|
| 翻译模型 | Claude API（tool use） | 藏文能力 + 长上下文放译例 + 工具调用 |
| 是否微调 | 否（至少第一期） | 数据量不足，RAG 已覆盖核心需求 |
| 词典/术语存储 | SQLite + FTS5 | 零运维、单文件、全文检索够用 |
| 术语表 | 人工可编辑的 TSV | 它是权威源，必须让人直接改 |
| 译例检索 | 向量索引（本地即可） | 数据量小，faiss/sqlite-vec 均可 |
| 审校格式 | 双栏对照 docx/md | 贴近译经审校的实际工作方式 |

---

## 六、里程碑

- **M1（第 1 周末）**：素材入库完成，词典可查询，至少一部译本完成句级对齐。
- **M2（第 2~3 周）**：术语表 v1（数百条人工确认），检索索引就绪。
- **M3（第 3~4 周）**：流水线跑通，能端到端翻译一品并输出审校稿。
- **M4（第 5 周起）**：审校闭环运转，评测基线建立，进入迭代。

---

## 七、第一步行动

把素材（脱敏后哪怕先给样例片段）放入 `data/raw/`，
从 `pipeline/normalize.py` 和 `parse_dict.py` 开始写起——
词典格式确定后，后面每一步都有明确依托。
